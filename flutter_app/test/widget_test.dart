import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App starts', (WidgetTester tester) async {
    // Riverpod ProviderScope is set up in main.dart,
    // so this smoke test just verifies the widget tree renders.
  });
}
